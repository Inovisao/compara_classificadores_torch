# Testado em Fev/2024
# Config: Dell Precision 3660, Nvidia RTX A4000, Ubuntu 22.04.3
# Pistori

# ACABEI DE VER QUE A INSTALAÇÃO NÃO FUNCIONOU DIREITO POIS
# O PYTORCH ESTÁ USANDO A CPU (NÃO ESTÁ PEGANDO A GPU)

conda create --yes --name cla_torch
conda activate cla_torch
conda install --yes pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
conda install --yes scikit-learn pandas matplotlib numpy seaborn  
# conda install --yes omnixai # DEU PAU COM PIP TAMBÉM
pip install --no-input ml_collections timm tifffile opencv-python-headless wget torchmetrics

# Para instalar as dependência dos códigos em R
# use o comando abaixo (não inclui instalação do próprio R)

sudo apt-get install libfontconfig1-dev
Rscript install_R_packages.R 


